{
  "name": "lrmj-project",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "LRMJ Project — Artisan ferronnier Bruxelles/Opwijk. Site vitrine sur mesure.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "deploy": "vite build && wrangler pages deploy dist",
    "cf:project": "wrangler pages project create lrmj-project --production-branch main"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "wrangler": "^4.93.1"
  },
  "dependencies": {
    "lenis": "^1.1.13"
  }
}
